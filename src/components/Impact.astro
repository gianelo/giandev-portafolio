---
export interface Props {
  lang: string;
  t: (key: string) => string;
}

const { t } = Astro.props;

const cards = [
  { key: 'years',    icon: '⏱', color: 'hover:border-yellow-400/60 dark:hover:border-yellow-500/60' },
  { key: 'pages',    icon: '📄', color: 'hover:border-blue-400/60 dark:hover:border-blue-500/60' },
  { key: 'servers',  icon: '🖥',  color: 'hover:border-emerald-400/60 dark:hover:border-emerald-500/60' },
  { key: 'payments', icon: '💳', color: 'hover:border-violet-400/60 dark:hover:border-violet-500/60' },
];
---

<section id="impact" class="py-20 px-6">
  <div class="max-w-4xl mx-auto">

    <!-- Section heading -->
    <div class="reveal flex flex-col items-center mb-14 text-center">
      <span class="text-4xl mb-3">📈</span>
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white">
        {t('impact.title')}
      </h2>
      <div class="mt-3 w-12 h-1 rounded-full bg-yellow-400"></div>
    </div>

    <div class="grid grid-cols-2 lg:grid-cols-4 gap-5">
      {cards.map((card, i) => (
        <div
          class={`reveal reveal-delay-${i + 1} group relative bg-white dark:bg-gray-900/60 border border-gray-200 dark:border-gray-800 rounded-2xl p-6 text-center ${card.color} transition-all hover:-translate-y-1 hover:shadow-lg hover:shadow-black/5 dark:hover:shadow-black/20`}
        >
          <div class="text-3xl mb-3">{card.icon}</div>
          <div class="text-3xl md:text-4xl font-bold text-yellow-500 dark:text-yellow-400 mb-2 tabular-nums">
            {t(`impact.${card.key}.number`)}
          </div>
          <p class="text-xs md:text-sm text-gray-500 dark:text-gray-400 font-medium leading-snug">
            {t(`impact.${card.key}.label`)}
          </p>
        </div>
      ))}
    </div>

  </div>
</section>
