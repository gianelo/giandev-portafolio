---
export interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="reveal flex flex-col items-center mb-14 text-center">
  <h2 class="text-3xl md:text-4xl font-bold text-zinc-900 dark:text-white">
    {title}
  </h2>
  <div class="mt-3 w-12 h-1 rounded-full bg-yellow-400"></div>
  {subtitle && (
    <p class="mt-4 text-sm md:text-base text-zinc-500 dark:text-zinc-400 max-w-xl">
      {subtitle}
    </p>
  )}
</div>
